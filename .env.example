# --- 前端构建时（Vite）---
# 生产环境：页面与 /api 同域时务必留空，浏览器会请求 https://你的域名/api/... ，由 Nginx 反代到 Node。
# 若错误地填了外网不可达的地址，会导致「邮件 API 超时」。
# VITE_EMAIL_API_BASE=

# 仅当邮件 API 与前端不同源时才填写完整基址（无末尾斜杠），例如：
# VITE_EMAIL_API_BASE=http://localhost:3001

# --- 服务器上 Node 邮件 API（systemd / 环境变量，不是 Vite）---
# 监听端口（默认 3001）。若 EADDRINUSE，换一个端口并同步 Nginx upstream（install.sh 可用 EMAIL_API_PORT）
# PORT=3001
# OMNITOOLS_LISTEN_HOST=127.0.0.1
# OMNITOOLS_DB_PATH=/path/to/omnitools.sqlite
# OMNITOOLS_UPLOAD_DIR=/path/to/upload
# SMTP_SECRET=随机长字符串

# 订阅追踪：CAD 折合 USD 时使用的汇率（1 USD 兑多少 CAD）。默认 1.37。
# OMNITOOLS_FX_CAD_PER_USD=1.37

# 部署后在服务器自检（应返回 JSON）：
# curl -sS http://127.0.0.1:3001/api/health
