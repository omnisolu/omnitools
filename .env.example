# Email API Base URL (optional)
# If running in development with separate backend server, set this to the backend URL
# In production with the install.sh deployment, leave empty or comment out
# VITE_EMAIL_API_BASE=http://localhost:3001
